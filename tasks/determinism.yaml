# Schema: docs/TASK_DAG.md    Validate: python3 tools/check_task_dag.py

plan: determinism
title: Deliberate changes to what the simulation computes, each gated on a smoke test
summary: >
  Every other plan under tasks/ is refactoring, held to AGENTS.md's standing
  rule that a change must not alter what the simulation computes. This plan is
  where that rule is broken on purpose: a change here is a behaviour change,
  which is why each one lands alone, in its own commit, and is verified by an
  owner-run Garden smoke test rather than by a green build.

  Nothing here may ride along inside a conversion. A modernisation diff and a
  deliberate behaviour change in the same commit produce a review in which
  neither can be judged, and a smoke test afterwards that is testing two things
  at once.

  SCOPE NOTE, because the plan name overpromises: this is not a determinism
  audit. Add a FIX here only when the change is understood well enough to state
  what it alters — T1 and T2 are that shape. Anything less understood is an
  investigation, and an investigation gets a SCOPING task whose deliverable is
  an answer rather than a diff.

  REOPENED 2026-08-05 on the owner's decision, after being archived when T1 and
  T2 closed. Two findings in AGENTS.md's Known issues had no owning task and
  had sat unowned across three batches, which is how a finding becomes
  folklore. They are T3 and T4 and they are SCOPING tasks: neither may change
  behaviour, and each ends with an answer written into this file. Whether
  either becomes a fix is a separate decision, taken after its answer exists.

  The note this replaces said both were "deliberately NOT tasks here, because
  neither has been investigated far enough to be planned". That reasoning was
  right about the fixes and wrong about the investigations — the investigation
  is precisely what can be planned, and it is the thing nobody was doing.

  ONE QUESTION STAYS OUT, and deliberately: cross-architecture float agreement
  between an ARM64 and an x64 client. AGENTS.md records that as decided rather
  than open — mixed-architecture play is not supported, on the owner's call of
  2026-08-03 — so it is not a finding awaiting investigation.

tasks:
  - id: T1
    title: Collapse the Spirit hover clamp to a single synchronised draw
    intent: >
      Spirit::Advance clamps m_hover.y in StateBirth and StateDeath through
      what used to be the max macro, which evaluated its second argument twice.
      That argument calls syncfrand(), so when m_hover.y loses the comparison a
      SECOND value is drawn and stored — the expression does not compute a
      maximum, and it consumes an extra value from the synchronised stream
      roughly half the time. Writing it as std::max makes it mean what it says
      and draws once. That is a change to what the simulation computes, which
      is why it is here and not in language-hygiene.
    project: GameLogic
    depends_on: []
    files:
      - GameLogic/Spirit.cpp
    acceptance:
      - Both sites read std::max(m_hover.y, ...) with exactly one syncfrand call each
      - The written-out conditional and the comment explaining why it was preserved are both gone
      - The two clamp statements hold two syncfrand calls between them, not four
      - The commit message states plainly that the RNG sequence changes from this point in the tick, and that a client with this change desyncs against one without
    verify:
      - python3 tools/check_format.py
      - msbuild Species.slnx /p:Configuration=Debug /p:Platform=x64 /m
      - vstest.console.exe x64\Debug\*Tests.dll
    parallel_safe: false
    status: done
    notes: |
      Found 2026-08-03 by language-hygiene T8, which preserved both sites
      exactly rather than decide this. The current code in the tree is the
      macro's expansion written out by hand, with a comment saying why —
      deliberately, so that nobody "simplifies" it back into std::max without
      knowing what that does.

      THIS IS NOT A DESYNC RISK, and the distinction matters because the shape
      of the bug invites the opposite conclusion. The number of draws depends on
      `m_hover.y > 0.0f + syncfrand(0.5f)`, and m_hover.y is
      sinf(m_positionOffset) * m_yaxisRate — pure simulation state. Every client
      whose simulation is in sync evaluates the same comparison, takes the same
      branch and draws the same number of values. It is self-consistently
      deterministic.

      Contrast SoundInstance.cpp, which AGENTS.md lists beside it: there the
      draw count depends on client-local sound configuration, so two clients
      that differ in what samples loaded consume different numbers of values.
      That one is a genuine desync. This one is not, and the two should not be
      fixed together or described in the same sentence.

      CORRECTION, 2026-08-05: the paragraph above is wrong about SoundInstance
      and is kept only because the contrast it draws is what T3 went and
      checked. The sound draws are on speciesRandom(), which is NOT the
      synchronised stream — syncrand() is, and they share no state. So the
      draw count there varies and does not matter. Everything this note says
      about T1 itself stands: m_hover.y is simulation state on the syncrand
      stream, and this fix does change the sequence. See T3's answer.

      WHY FIX IT AT ALL, then. Three reasons, in order of weight: it is a trap —
      `(a > b) ? a : b` over a call with a side effect is exactly what the next
      tree-wide sweep will collapse without noticing; m_hover.y feeds m_pos
      (Spirit.cpp:146) which feeds GenerateSyncValue, so this is simulation
      state rather than a rendering offset; and it wastes a draw.

      WHAT CHANGES, stated so the smoke test knows what it is looking at. Every
      syncrand() after the first affected Spirit in a tick returns a different
      value, so the simulation evolves differently from the frame this lands.
      Nothing degrades — there is no correct sequence being lost — but it is not
      the same game state it would otherwise have been.

      The visible effect is expected to be nil. The clamp's intent survives
      either way: the result lands in [0, 0.5) for birth and [2, 4) for death
      whichever branch is taken, which is why this has gone unnoticed since the
      code was inherited.

      WHAT IT COSTS, measured rather than assumed: no replay or recorded-input
      system exists to pin the sequence (the "playback" hits in the tree are
      camera animation), nothing persisted stores RNG state — level files hold
      positions — and the wire format is unaffected. The only real cost is that
      a client with this change desyncs against one without, and there is no
      shipped build and no multiplayer in use. That cost only grows with time,
      which is the argument for doing it sooner.

      parallel_safe false, and more strictly than the flag can express: nothing
      else touching simulation code may land between this and T2, or the smoke
      test is testing two things.

      OWNER DECIDED 2026-08-03: fix it, over leaving it permanently or deferring
      until multiplayer is tested. The argument that carried it was that the diff
      gets no cheaper and the branch is at its most verifiable now.

      DONE. Both sites are `std::max(m_hover.y, ...)` with one syncfrand each;
      the written-out conditionals and their explanatory comments are gone.
      syncfrand occurrences in the file go from 10 to 7 — the two clamps drop
      from four calls to two, and a comment naming the function went with them.

      THE ACCEPTANCE LINE I WROTE HERE WAS WRONG and is corrected above. It said
      `grep -c` would fall from 6 to 4; grep -c counts LINES, the file had 8 of
      them, and one of those was a comment mentioning the function. Countable
      acceptance is only worth having if the count is measured rather than
      guessed at when the task is written. Worth remembering when writing the
      next one.

      T2 is the gate. This is a change to what the simulation computes and
      nothing that runs in CI can see it.

  - id: T2
    title: Owner runs the Garden smoke test against the changed stream
    intent: >
      A green build says nothing about a changed RNG sequence, and neither does
      the unit suite — nothing in Tests/ reaches Spirit or Location::Advance.
      The smoke test is the only evidence available that the simulation still
      behaves, and T1 is not done without it.
    depends_on: [T1]
    files:
      - AGENTS.md
    acceptance:
      - All seven Garden smoke-test steps pass on the changed build, owner-reported
      - AGENTS.md records the run with the commit hash, per "What working looks like"
    status: done
    notes: |
      Blocked on owner execution — agents in this environment cannot launch a
      Windows client, and no check that runs here can substitute. Same gate and
      the same reason as rename-darwinian T4.

      WHICH STEPS CARRY THE WEIGHT, which is different from the rename's answer.
      There the spawn counts were the whole test, because a missed string
      produces a smaller group. Here the counts prove nothing — the rename is
      long landed and spirits are not spawned from InstantUnits. What matters is
      step 5, thirty seconds of play with no assert, and step 7, the sync
      assert in single player: together they show the simulation is still
      self-consistent after the stream shifted. Step 2 covers the load path.

      If a step does fail, it localises well. Spirits are visible in The Garden
      and their hover is the only thing this changes, so "spirits behave" and
      "nothing asserts" are the two observations worth writing down.

      DONE 2026-08-05 at bd03d4e, owner-reported: "Smoke test finished
      successfully". T1's change to Spirit's hover clamp -- one synchronised
      draw where there had been two -- has now been in front of a running game.

      CLOSED BY THE SAME RUN AS directxmath/T21, deliberately. Both gates are
      the same seven Garden steps on the same build, and by the time the run
      happened Spirit.cpp carried T1's RNG change AND its math conversion, so
      one run is the only way to test either of them honestly. Recorded on both
      tasks and in AGENTS.md.

  - id: T3
    title: Establish what SoundInstance's two synchronised draws cost
    intent: >
      NeuronClient/SoundInstance.cpp calls speciesRandom() twice — once to pick
      a sample from a group, once to pick an object id — and both sit on paths
      whose execution depends on CLIENT-LOCAL sound configuration. If two
      clients draw a different NUMBER of values from the shared stream, every
      subsequent speciesRandom() in the simulation diverges and the game
      desyncs with every build green. Nobody has established whether that can
      actually happen. This task answers that and only that: it is a reading
      task, its deliverable is the answer written into these notes, and it
      changes no behaviour.
    project: NeuronClient
    depends_on: []
    files:
      - NeuronClient/SoundInstance.cpp
      - NeuronClient/SoundSystem.cpp
    acceptance:
      - notes states, per call site, exactly which conditions decide whether it executes, and which of those are client-local
      - notes states whether two clients with different sound settings, different hardware, or a failed sample load can draw a different number of values — yes, no, or "not determinable by reading", each with the evidence
      - If the answer is that they can, notes names the cheapest fix and whether it changes the RNG sequence for everyone
      - No behaviour change and no diff outside comments in this task
    verify:
      - python3 tools/check_hygiene.py
      - python3 tools/check_format.py
    parallel_safe: true
    status: done
    notes: |
      Added 2026-08-05 on the owner's decision to reopen this plan. The finding
      itself is older: found by reading while scoping containers-replaced T5 on
      2026-08-02, written into AGENTS.md's Known issues, and unowned ever
      since — three batches went past it.

      WHAT IS ALREADY KNOWN, so this starts from the reading rather than
      repeating it. The two sites are SoundInstance.cpp:547 (pick a sample from
      a group) and :1035 (pick an object id) — measured at 97b5844; they were
      written down as :533 and :1009 and the file has grown since, so re-grep
      `speciesRandom` rather than trusting either pair. speciesRandom() is the
      deterministic lockstep RNG, the same sequence Location::Advance consumes,
      and CODING_STANDARDS.md#determinism names the call sequence as
      load-bearing. Whether either line runs depends on how many object ids an
      instance holds, whether a sample group is populated, and whether the
      instance is playing at all.

      THE DISTINCTION THAT MATTERS, and it is why T1 is not a precedent here.
      T1's double draw was NEVER a desync risk: its branch depended on
      m_hover.y, which is simulation state, so every in-sync client drew the
      same number of values. These two are the dangerous shape — the draw count
      varies with client-local configuration. Read T1's notes before assuming
      the two are the same bug.

      ownership/T8 converts m_sounds in this same file and is now unblocked,
      so whoever takes that task is already in the right place to take this
      one. They are different changes and want different commits.

      ================= THE ANSWER, 2026-08-05 at 9d716f6 =================

      THE PREMISE IS WRONG. speciesRandom() IS NOT THE LOCKSTEP STREAM, and
      everything above that calls it one — including AGENTS.md's Known-issues
      bullet, which is corrected in the same commit as this note — inherited
      that claim without checking it. The tree has TWO generators with no
      shared state:

        speciesRandom()  NeuronCore/Random.cpp   a 32-bit LCG over one static
                                                 `holdrand`. frand() and
                                                 sfrand() are built on it.
        syncrand()       NeuronCore/MathUtils.cpp  a Mersenne Twister with its
                                                 own mt[]/mti state.
                                                 syncfrand()/syncsfrand() are
                                                 built on it.

      MathUtils.h labels the second pair itself, and has since the code was
      inherited: "Network Syncronised versions (only call inside Net-Safe
      code)". The simulation obeys that label — syncrand appears in 40+
      GameLogic files and 58 times in Citizen.cpp alone, while speciesRandom
      appears in 19 files across the whole tree, most of them rendering or UI.
      So the two SoundInstance draws are on the UNSYNCHRONISED stream, and no
      number of them can move the synchronised one. There is no shared
      sequence to perturb.

      PER CALL SITE, as the acceptance asks. Both are client-local several
      times over, and the answer to "can two clients draw a different NUMBER
      of values" is an emphatic YES — it is simply not a desync, because of
      the stream.

      Site 1, SoundInstance::OpenStream (:547). Executes only when: the
      instance has no cached sample handle, or the caller passed
      _keepCurrentStream=false and the source is not a plain Sample (:522);
      AND m_sourceType == SampleGroupRandom (:534). The modulus is
      `numSamples`, scaled by the "SoundMemoryUsage" PREFERENCE — 2 halves it,
      3 quarters it (:539-543) — so two clients drawing at the same point get
      DIFFERENT VALUES as well as, potentially, different counts. Reachable
      from the preferences UI: PrefsSoundWindow.cpp:68 calls OpenStream when
      the user changes a sound setting.

      Site 2, SoundInstance::ResolveAttachedObject (:1035). Executes only
      when: m_positionType == Type3DAttachedToObject; g_locationId != -1 and
      g_locationAccess is set; a recalculation is due — the attached object
      died, OR m_instanceType == MonophonicNearest, OR (MonophonicRandom AND
      m_restartOccured); m_objIds is non-empty; and m_instanceType is
      MonophonicRandom. m_restartOccured is set by OpenStream, so the same
      preferences-window path arms this one too.

      AND THE WHOLE SUBSYSTEM IS GATED CLIENT-LOCALLY, which settles the count
      question before either site is reached. SoundSystem::Advance returns
      immediately `if (!m_channels)` (:1276), and m_channels is allocated only
      after the sound library initialises (:319) — a client with no audio
      device, or with sound off, draws ZERO values all session. Past that gate
      the body runs on `m_timeSync += g_advanceTime` against a 0.05s period
      (:1284), and g_advanceTime is the WALL-CLOCK LENGTH OF THE LAST FRAME
      (NeuronCore/GameTime.h:8), clamped to 0.25. At most one period is
      consumed per call, so a client below 20fps advances the sound system
      once per frame and a faster one advances it 20 times a second. The
      number of draws is frame-rate dependent by construction.

      SO: NOT A DESYNC, AND THE FINDING RELOCATES RATHER THAN CLOSING. What
      makes the draw count harmless is the stream, not the sound system — and
      four sites in GameLogic compute SIMULATION STATE from that same
      unsynchronised stream:

        Spider.cpp:354    picks a neighbour with speciesRandom() and writes
                          m_targetPos from it
        Armour.cpp:253    same shape, writes m_wayPoint
        ArmyAnt.cpp:185   `while(true){ newIndex = speciesRandom()%3; if
                          (newIndex != currentIndex) break; }` — a rejection
                          loop, so even the COUNT it draws is unbounded
        Explosion.cpp:144 picks a tumbler for each debris fragment

      Entity positions and velocities are what GenerateSyncValue sums
      (Main.cpp:252-330, Debug only; in Release the sync value is
      `255 * syncfrand()`), so the first two feed the checksum directly. That
      is the real hazard, and it is the mirror image of the one recorded: it
      is not that sound perturbs the simulation's stream, it is that a handful
      of simulation sites read from the stream sound is allowed to perturb.
      Any client-local consumer reaches them — sound, Eclipse's four draws,
      LandscapeRenderer (T4), CameraAnimWindow, TreeWindow.

      THE CHEAPEST FIX, and it is not in SoundInstance.cpp. Change those four
      GameLogic sites from speciesRandom() to syncrand(), which is what the
      header already tells them to use inside net-safe code. Nothing in
      NeuronClient needs to change: sound, UI and rendering are entitled to
      the unsynchronised generator and should keep it.

      DOES IT CHANGE THE RNG SEQUENCE FOR EVERYONE? Yes, on both streams — the
      four sites stop consuming from holdrand and start consuming from the
      Mersenne Twister, so every subsequent draw on either stream shifts. That
      makes it exactly T1's shape: a deliberate behaviour change, landing
      alone, gated on an owner-run smoke test. It is NOT part of this task,
      which changes nothing. It wants its own node, and the evidence for it is
      this note.

      WHAT THIS DOES NOT SAY. It does not say the sound system is well
      behaved: reseeding and drawing from a process-global generator out of
      rendering and UI code is why this question was expensive to answer.
      It says the specific desync alleged in AGENTS.md cannot happen by the
      mechanism alleged, and names the four places where an equivalent one
      can.

  - id: T4
    title: Establish what GetLandscapeColour's reseed costs
    intent: >
      LandscapeRenderer::GetLandscapeColour calls
      speciesSeedRandom(_x | _y + speciesRandom()) — it both draws from and
      RESEEDS the simulation's random stream, from rendering code, on a path
      whose execution depends on what is on screen and how often the frame
      loop runs it. That is the same class of latent desync as T3 and
      potentially worse, because a reseed discards the stream rather than
      advancing it. As with T3 this task establishes what it costs and changes
      nothing.
    project: GameLogic
    depends_on: []
    files:
      - GameLogic/LandscapeRenderer.cpp
    acceptance:
      - notes states what calls GetLandscapeColour, how often, and whether the count is frame-rate or view dependent
      - notes states whether the reseed is reachable while a simulation tick is in progress, and what the shared stream's state is afterwards
      - notes answers whether this is benign, a real desync source, or not determinable by reading — with the evidence for whichever
      - No behaviour change and no diff outside comments in this task
    verify:
      - python3 tools/check_hygiene.py
      - python3 tools/check_format.py
    parallel_safe: true
    status: done
    notes: |
      Added 2026-08-05 on the owner's decision to reopen this plan. Found
      2026-08-04 while investigating the Garden landscape question, unrelated
      to it, and it predates the DirectXMath work.
      _restart-directxmath.md said it "belongs in tasks/determinism.yaml once
      somebody establishes what it costs" — which is circular, since
      establishing that is the work. This task is the way out of that loop.

      DO NOT CONFUSE THIS WITH THE LANDSCAPE-SHAPE QUESTION, which is closed.
      Terrain generation reseeds immediately before its diamond-square loop and
      is a pure function of the level's m_randomSeed and its tile parameters;
      the height-map checksums were equal across both builds and 57386fb, the
      commit that measured it, has been reverted. This is the RENDERER's colour
      lookup, a different function, and nothing about the terrain answer says
      anything about it.

      A CHEAP FIRST STEP, if it helps: the expression is
      `speciesSeedRandom(_x | _y + speciesRandom())`, whose precedence is
      `_x | (_y + speciesRandom())` rather than the `(_x | _y) + …` a reader
      expects. That is worth noting in the answer either way, but it is not the
      question — the question is the reseed.

      THE DECLARED PATH WAS WRONG AND IS CORRECTED, 2026-08-05 at 97b5844.
      This task was written against `Species/LandscapeRenderer.cpp`, which does
      not exist: layering-inversion moved the renderer into GameLogic with the
      rest of the world model, and the file is
      `GameLogic/LandscapeRenderer.cpp` — the call is at line 169 and the only
      caller inside the file at line 216. `project` was `Species` for the same
      reason and is now GameLogic. Nothing caught this, and that is the point
      worth carrying: check_task_dag.py validates the GRAPH, not that a
      declared file is on disk, so a path that rots when a file moves stays
      valid forever. An agent starting this task would have opened a missing
      file first.

      ================= THE ANSWER, 2026-08-05 at 2258661 =================

      BENIGN, ON TWO INDEPENDENT GROUNDS, EITHER OF WHICH IS SUFFICIENT. Both
      of the premises this task inherited are wrong, and they are wrong in
      different ways.

      GROUND 1 — IT IS THE WRONG STREAM, exactly as T3 found for the sound
      draws. speciesSeedRandom/speciesRandom are the LCG in
      NeuronCore/Random.cpp; the simulation runs on syncrand's Mersenne
      Twister and the two share no state. Reseeding holdrand cannot touch a
      synchronised sequence, because holdrand is not one. Read T3's answer
      first — it establishes this once for both tasks.

      GROUND 2 — IT IS NOT ON A FRAME PATH AT ALL. The task says execution
      "depends on what is on screen and how often the frame loop runs it".
      It does not. The full call graph, measured:

        LandscapeRenderer::GetLandscapeColour  (:164, the reseed at :169)
          <- LandscapeRenderer::BuildColourArray  (:192, the only caller,
             at :216, inside a loop over triangle-strip vertices)
            <- LandscapeRenderer::BuildOpenGlState  (:287)
              <- LandscapeRenderer::LandscapeRenderer  (:269)
                <- Landscape::BuildOpenGlState  (Landscape.cpp:527, `new
                   LandscapeRenderer(m_heightMap)`)

      GetLandscapeColour has ONE caller, and it is a one-time colour bake
      that runs when the landscape is built. It is not called from Render(),
      not from any Advance, and its iteration count is the vertex count of
      the terrain mesh — a function of the level, identical on every client
      loading that level. Nothing about it is frame-rate or view dependent.

      IS THE RESEED REACHABLE WHILE A SIMULATION TICK IS IN PROGRESS? No.
      It runs during location load, before the tick loop starts. And the
      stream's state afterwards is settled by the level anyway: Location::Init
      calls speciesSeedRandom(1) at Location.cpp:136, immediately after
      LoadLevel and before InitLandscape, so the unsynchronised stream is
      re-anchored per location regardless of what the colour bake left in it.

      WHAT REMAINS TRUE AND IS WORTH A LINE. The expression is
      `speciesSeedRandom(_x | _y + speciesRandom())`, which parses as
      `_x | (_y + speciesRandom())` rather than the `(_x | _y) + …` a reader
      expects — recorded because the next person will read it the same wrong
      way, not because it is a defect. The comment above the function says x
      and y are "only passed in as a means to get predicatable noise", and
      that is exactly what it does: it makes terrain colour a repeatable
      function of position. Reseeding a process-global generator to get
      per-pixel noise is a poor way to spell that, and a local generator
      would be better, but it is a tidiness question and this plan is not
      where tidiness lands.

      NO FIX IS PROPOSED AND NONE IS NEEDED. Unlike T3, this one does not
      relocate: nothing downstream of the colour bake computes simulation
      state. The four sites T3 found are the whole of that hazard, and they
      are T5.

  - id: T5
    title: Move the four simulation-side draws onto the synchronised stream
    intent: >
      Four GameLogic sites compute simulation state from speciesRandom(), the
      UNSYNCHRONISED generator, while everything around them uses syncrand().
      Two of the four write entity positions, which GenerateSyncValue sums, so
      any client-local consumer of that generator — sound, the Eclipse UI, the
      landscape renderer — changes what the simulation computes. This is the
      real hazard behind the finding T3 was written to investigate.
    project: GameLogic
    depends_on: [T3, T4]
    files:
      - GameLogic/Spider.cpp
      - GameLogic/Armour.cpp
      - GameLogic/ArmyAnt.cpp
      - GameLogic/Explosion.cpp
    acceptance:
      - The four sites named in notes draw from syncrand() and no simulation
        state in GameLogic is computed from speciesRandom() or from frand/sfrand
      - The remaining speciesRandom callers are listed in the commit with, for
        each, one line saying why it is not simulation state
      - ArmyAnt's rejection loop draws a bounded number of values, or the commit
        states why an unbounded loop on the synchronised stream is acceptable
      - The commit states plainly that the RNG sequence changes on BOTH streams
        from this point, and that a client with this change desyncs against one
        without
      - Owner runs the Garden smoke test — spiders, armour, army ants and
        explosions are all in it
    verify:
      - python3 tools/check_format.py
      - msbuild Species.slnx /p:Configuration=Debug /p:Platform=x64 /m
      - vstest.console.exe x64\Debug\*Tests.dll
    parallel_safe: false
    status: todo
    notes: |
      Added 2026-08-05 as T3's deliverable. T3 was a scoping task and this is
      the fix it names; read T3's answer before starting, because the argument
      for this task is entirely in it.

      THE FOUR SITES, measured at 9d716f6:
        Spider.cpp:354     neighbour pick -> m_targetPos
        Armour.cpp:253     neighbour pick -> m_wayPoint
        ArmyAnt.cpp:185    rejection loop -> m_shape
        Explosion.cpp:144  debris tumbler pick

      Spider and Armour are the two that reach the checksum: both write a
      target the entity then moves toward, and GenerateSyncValue sums entity
      m_pos and m_vel. ArmyAnt's is a shape swap and Explosion's is debris, so
      neither is obviously summed — they are in scope anyway because
      ArmyAnt's loop draws an UNBOUNDED number of values from a shared
      generator, and because leaving two of four converted is the kind of
      half-state that reads as deliberate later.

      IT NEEDS A DECISION BEFORE IT NEEDS AN AGENT, and the decision is the
      owner's: this is a behaviour change of T1's exact shape. It is written
      as todo rather than blocked because nothing technical stands in the way
      — but do not start it on the strength of this note alone.

      ONE TRAP. `frand()` and `sfrand()` are speciesRandom in disguise
      (MathUtils.h:9-10) and read as innocuous. Any GameLogic site computing
      simulation state through those is the same bug; the acceptance above
      says so, and a grep for the two names is not optional.
